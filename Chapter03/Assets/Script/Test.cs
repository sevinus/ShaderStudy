using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
        float value = 100.0f;
        float reuslt = Mathf.Pow(value, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
